name: prime
on: [push, pull_request]
jobs:
   test:
     name: Test
     runs-on: windows-latest
     
     steps:
        - uses: actions/checkout@v3.10.0
        - name: Setup Java JDK 11
          uses: actions/setup-java@v3.10.0
          with:
            java-version: 3.10.0

        - name: setup java
          run: |
                javac prime.java
                java prime
