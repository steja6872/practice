name: prime
on: [push]
jobs:
   test:
     name: Test
     runs-on: [windows-latest]
     
     steps:
        - uses: actions/checkout@v2
        - name: Setup Java
          uses: Fleshgrinder/setup-java@1.0.0
          with:
            version: 1.0.0
    
  
        - name: setup java
          run: |
                javac prime.java
                java prime
